<% provide(:title, "あなたの記録") %>
<h1>
  ユーザ名：<%= @user.name %>
</h1>
<% @records.each do |record| %>
  <li>stage：<%= record.stage_id %>, weapon：<%= record.weapon_id %>, hazard_level：<%= record.hazard_level %></li>
<% end %>